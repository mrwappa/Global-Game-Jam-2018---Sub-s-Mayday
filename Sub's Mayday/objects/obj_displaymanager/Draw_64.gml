/// @descriptions
draw_set_alpha(1);
draw_set_halign(fa_right);


draw_text(display_get_gui_width()-5, +30, view_zoom);	
/*if(gamepad_get_device_count() > 0)
{
	for (var i = 0; i < gamepad_get_device_count(); ++i) 
	{
	    if(gamepad_button_check(i,gp_start))
		{
			
		}
	}
}*/
